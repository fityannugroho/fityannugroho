/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  /* config options here */
  i18n: {
    locales: ['en-US', 'id-ID'],
    defaultLocale: 'en-US',
  },
};

export default nextConfig;
